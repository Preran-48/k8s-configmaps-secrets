apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod-hc-env
spec:
  containers:
  - name: nginx-container
    image: nginx
    env:
    - name: APP_ENV
      value: production

    - name: APP_PORT
      value: "8080"
